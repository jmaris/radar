
Sonar has triggered an automatic alert because the RAM usage exceeds <%= @threshold %> % for <%= @machine_alias %>.

<% if @alert.custom_message.empty? == false %>
Below is the system administrator message:
<%= @custom_message %>
<% end %>