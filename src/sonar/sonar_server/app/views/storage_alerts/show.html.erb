<script>
  $(document).ready(function(){
    $('[data-toggle="tooltip"]').tooltip({ container: 'body'});   
  });
</script>

<div class="btn-group" role="group" aria-label="Actions">
  <%= link_to storage_alerts_path, class: 'btn btn-default', data: { toggle: 'tooltip', title: 'Back'} do %>
  <i class="glyphicon glyphicon-arrow-left"></i>
  <% end %>

  <%= link_to edit_storage_alert_path(@storage_alert), class: 'btn btn-default', data: { toggle: 'tooltip', title: 'Edit' } do %><i class="glyphicon glyphicon-edit"></i><% end %>
</div>

<p id="notice"><%= notice %></p>

<dl class="dl-horizontal">
  <dt>Machine ID</dt>
  <dd><%= @storage_alert.machine_id %></dd>

  <dt>E-Mail Address</dt>
  <dd><%= @storage_alert.addressee %></dd>

  <dt>Partition</dt>
  <dd><%= @storage_alert.path %></dd>

  <dt>Storage threshold</dt>
  <dd><% if @storage_alert.triggered %> <span class="label label-danger"> <% end %> <%= @storage_alert.threshold %> <% if @storage_alert.triggered %> % </span> <% else %> % <% end %> </dd>

  <dt>Check interval</dt>
  <dd><%= pluralize(@machine.update_interval, "minute") %></dd>

  <dt>Duration</dt>
  <dd><%= pluralize(@storage_alert.duration, "minute") %></dd>

  <dt>Cycles</dt>
  <dd><%= @storage_alert.duration / @machine.update_interval %></dd>
</dl>